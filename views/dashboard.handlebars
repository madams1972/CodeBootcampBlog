<div class="row">
  <div class="col-auto">
    {{#if userName.name}}
    <h2>Welcome, {{userName.name}}!</h2>
    {{else}}
    <h2>Log in or Sign Up</h2>
    {{/if}}
    <p>Welcome to the Code Bootcamp Blog where you can create, read, and edit your blog post.</p>
    <a href="/api/post/create">
      <button class="btn btn-md btn-dark" id="btn-create-post">Create Post</button>
    </a>
  </div>
</div>
<hr>
<div>
  <h3>My Posts</h3>
</div>
{{#if posts}}
  <div class="col-md-12 post-list">
    <!-- Loop over each object in the posts array -->
    {{#each posts as |post| }}
      <div class="card cardLink">
        <div class="card-body">
          <a href="/api/post/{{post.id}}">
            <h5 class="card-title">{{post.title}}</h5>
          </a>
          {{!-- Split into partial? --}}
          <p class="card-description">{{post.description}}</p>
          <span class="card-author">Created by {{post.user.name}} on
        {{format_date createdAt}}</span>
        </div>
      </div>
    {{/each}}
  </div>
  {{else}}
  <div>
    <p> Create a post to share your thoughts with the tech community!</p>
  </div>
{{/if}}