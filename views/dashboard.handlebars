<div class="row">
  <div class="col-auto">
    {{#if userName.name}}
    <h2>Welcome, {{userName.name}}!</h2>
    {{else}}
    <h2>Please Login or Sign Up</h2>
    {{/if}}
    <p>Welcome to Code Boot Camp Blog where anyone can post a story.</p>
    <a href="/api/post/create">
      <button class="btn btn-md btn-dark" id="btn-create-post">Create Post</button>
    </a>
  </div>
</div>
<hr>
<div>
  <h3>My Posts</h3>
</div>
{{#if posts}}
  <div class="col-md-12 post-list">
    <!-- Loop over each object in the posts array -->
    {{#each posts as |post| }}
      <div class="card cardLink">
        <div class="card-body">
          <a href="/api/post/{{post.id}}">
            <h5 class="card-title">{{post.title}}</h5>
          </a>
          {{!-- Split into partial? --}}
          <p class="card-description">{{post.description}}</p>
          <span class="card-author">Created by {{post.user.name}} on
        {{format_date createdAt}}</span>
        </div>
      </div>
    {{/each}}
  </div>
  {{else}}
  <div>
    <p>Welcome to Codde Bootcamp Blog. Start writing and don't forget to submit your post. You can edit it if you need to.</p>
  </div>
{{/if}}